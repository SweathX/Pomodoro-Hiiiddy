'use client';

import { ObjectiveDisplay } from '@/components';

export default function ObjectiveOverlay() {
  return (
    <main className="p-4 w-[500px]">
      <ObjectiveDisplay />
    </main>
  );
}
